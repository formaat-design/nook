{
  "name": "root",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "integrations/*",
    "examples/*"
  ],
  "scripts": {
    "build": "lerna run build --no-private",
    "app:dev": "open \"http://localhost:3000\" && lerna run dev",
    "app:build": "lerna run build --scope nook-app"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.0.0",
    "prettier": "^3.0.3",
    "typescript": "^5",
    "lerna": "7.4.2",
    "chokidar-cli": "3.0.0",
    "concurrently": "8.2.2"
  }
}
